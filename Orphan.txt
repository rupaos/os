orphan

#include <stdio.h>
#include <unistd.h>

int main() {
    int pid = fork();

    if(pid > 0) {
        printf("Parent Process\n");
    }
    else {
        sleep(5);
        printf("Child becomes Orphan\n");
    }

    return 0;
}